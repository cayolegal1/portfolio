"use client";
import { useLayoutEffect } from "react";

export const ScrollToTop = ({ }) => {
  useLayoutEffect(() => {
    // window.scrollTo({behavior: "instant", top: 0});
  }, []);
  return (
    <></>
  );
};